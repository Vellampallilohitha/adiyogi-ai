import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adiyogi.mahashivauniverse',
  appName: 'MahaShiva-Universe',
  webDir: 'build'
};

export default config;
